import React, {useState, useEffect, useContext} from 'react';
import {observer} from 'mobx-react';

import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text, TouchableOpacity,
  StatusBar,
} from 'react-native';

const Send = () => {
  return (
    <View style={{flex:1, backgroundColor:'#0088cc'}}>

    </View>
  )
}

export default observer(() => Send());
